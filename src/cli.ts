#!/usr/bin/env node

/**
 * periwinkle CLI entry point.
 *
 * Placeholder until the `build` and `preview` commands land
 * (https://github.com/phranck/periwinkle/issues/6). Prints a short notice and
 * exits with a non-zero code so scripts cannot silently depend on a no-op.
 */
console.error("periwinkle: the build and preview commands are not implemented yet.");
process.exit(1);
